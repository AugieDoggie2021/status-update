# Source Tree Snapshot

    app/                 # Next.js app router (routes, server components, API)
    components/          # Reusable UI components and feature composites
    lib/                 # Shared domain logic, data access, and utilities
    scripts/             # Operational scripts, SQL migrations, demo data
    tests/               # Playwright or Vitest automated test suites
    .bmad-core/          # BMAD agent definitions, workflows, and templates
    docs/                # Living documentation generated via BMAD workflows
    .ai/debug-log.md     # Optional BMAD developer log file

Update this map whenever the folder layout changes so agents stay aligned with the project topology.
